我正在执行“从 Java 后端转向 AI 应用开发”的就业作战计划，请你继续作为我的长期学习导师，按照既定路线指导我。

# 一、我的长期目标

我的长期目标是参加 2026 年秋招，目标岗位以以下方向为主：

1. AI 应用开发工程师；
2. 大模型应用开发工程师；
3. Java 后端 + AI 应用融合岗位；
4. 后端开发岗位中涉及 RAG、Agent、Function Calling、MCP、大模型接入的平台型岗位。

我的核心定位不是转向大模型算法研究或模型训练岗，而是：

> 以 Java 后端能力为工程基础，补齐 Python、LLM API、RAG、Agent、MCP 和 AI 应用工程能力，形成“后端工程能力 + 大模型应用能力”的差异化竞争力。

长期需要具备的能力包括：

- 能独立使用 Python 开发 AI 应用；
- 能理解并调用主流大模型 API；
- 能实现流式输出、多轮对话、结构化输出和 Function Calling；
- 能从零实现基础 RAG 系统；
- 能理解 Embedding、向量检索、文档切分、召回和重排序；
- 能使用 LangChain 或 LangGraph，但不能只会调用框架；
- 能实现简单 Agent，并理解工具调用、状态管理和任务编排；
- 能理解 MCP 的作用、客户端、服务端和工具暴露机制；
- 能将 AI 服务封装为 FastAPI 或后端服务；
- 能考虑异常处理、日志、限流、缓存、成本、延迟和可观测性；
- 能将 AI 能力与现有 Java 项目结合，形成可写入简历、可在面试中深挖的完整项目；
- 同时保留 Java、MySQL、Redis、JVM、JUC、RabbitMQ 和算法能力。

# 二、短期目标

当前短期目标是完成 AI 应用开发的基础阶段，并尽快形成一个可以持续迭代的项目。

接下来的学习顺序为：

1. 模型返回结果的结构化封装；
2. 流式输出；
3. 多轮对话与上下文管理；
4. 结构化输出；
5. Function Calling；
6. Embedding 基础；
7. 文档加载、切分和向量化；
8. 基础 RAG；
9. RAG 召回质量、重排序和评估；
10. Agent；
11. LangGraph；
12. MCP；
13. FastAPI 服务化；
14. 与 Java 后端项目集成；
15. 项目优化、压测、面试表达和简历包装。

短期内不要直接跳到 LangChain、Agent 或复杂框架，而应先使用原生 SDK 理解底层调用链路。

# 三、当前已完成进度

我已经完成 Day 01 的学习：

## Python 基础

- Python 使用缩进表示代码块；
- 函数定义、返回值和类型注解；
- 默认参数、关键字参数和 `None`；
- 理解可变默认参数的风险；
- List、Dict、Tuple、Set；
- `enumerate()`；
- `__name__ == "__main__"`；
- `import` 和多模块拆分；
- 项目统一使用 4 个空格缩进；
- 使用 VS Code 和 Black Formatter。

## 工程结构

已经能够将代码拆分为：

```text
config.py
llm_client.py
main.py
```

理解了：

- `config.py` 负责配置；
- `llm_client.py` 负责模型调用；
- `main.py` 负责用户输入和结果展示。

## 环境变量

已经完成：

- `.env`；
- `python-dotenv`；
- `load_dotenv()`；
- `os.getenv()`；
- `get_required_env()`；
- 将字符串配置转换为 `float`；
- 将 API Key、Base URL、模型名称和 System Prompt 与代码分离；
- `.env` 加入 `.gitignore`。

## LLM API

已经使用 OpenAI Python SDK 成功调用 OpenRouter 的 OpenAI 兼容接口。

当前技术链路为：

```text
用户输入
→ main.py
→ llm_client.py
→ OpenAI Python SDK
→ OpenRouter
→ 免费模型
→ ChatCompletion
→ 提取模型回答
```

已经理解：

- OpenAI SDK 只是客户端，不等于 OpenAI 官方模型；
- 真正决定服务的是 Base URL、API Key 和 Model；
- 可以通过配置切换不同兼容服务。

## Response 解析

已经理解并实际观察：

```python
response.choices[0].message.content
```

并分析过以下字段：

- `model`
- `choices`
- `message`
- `content`
- `finish_reason`
- `usage`
- `prompt_tokens`
- `completion_tokens`
- `total_tokens`
- `reasoning_tokens`
- `provider`
- `tool_calls`
- `cost`

已理解：

- `choices[0]` 表示第一个候选结果；
- `finish_reason="stop"` 表示正常结束；
- `finish_reason="length"` 可能表示回答被截断；
- Prompt Token 不仅包含用户输入，还包括 System Prompt 和历史消息；
- `openrouter/free` 是路由名称，实际模型可能是其他免费模型；
- 业务代码不应依赖非通用的 reasoning 字段。

## 异常排查

曾遇到 OpenAI 官方 API 返回：

```text
429 insufficient_quota
```

已经确认：

- 不是 Python 代码错误；
- 不是网络错误；
- 是 API 账户没有额度；
- 后续切换到 OpenRouter 免费模型并成功调用。

当前程序已经具备：

- 空输入校验；
- 基础异常捕获；
- 模型调用失败时输出错误。

# 四、下一阶段优先任务

下一次对话请从以下任务开始，并按优先级推进：

## P0：统一封装模型返回结果

将当前只返回字符串的 `ask_model()` 改造成返回结构化结果，例如包含：

- `answer`
- `model`
- `finish_reason`
- `usage`

需要让我先独立思考使用 Tuple、Dict 或 `dataclass`，然后再检查我的实现。

## P1：Streaming

学习并实现：

- `stream=True`
- 增量读取内容
- 控制台逐步输出
- 同时拼接完整回答
- 理解首 Token 延迟和总响应时间的区别

## P2：多轮对话

实现：

- `system`
- `user`
- `assistant`

组成的历史消息列表；

让模型能够基于之前的内容继续回答。

同时理解：

- 大模型本身通常不主动记忆会话；
- 上下文由客户端重新发送；
- 历史越长，Token 和成本越高；
- 后续需要裁剪、摘要或记忆机制。

## P3：面试表达

每完成一个模块，都要整理对应的面试问题和自然表达，例如：

- 流式输出和普通输出有什么区别？
- 为什么流式输出能降低用户感知延迟？
- 多轮对话是如何实现的？
- 为什么历史消息会增加 Token？
- 为什么需要对模型返回结果进行封装？

## P4：算法训练

每天至少完成一道算法题，控制在 30～60 分钟。

优先保证：

- 独立分析；
- 独立编码；
- 调试；
- 复盘错误。

# 五、学习方式要求

请继续采用以下方式指导我：

```text
知识点讲解
→ 小实验
→ 我先独立编码
→ 你检查代码
→ 指出错误和改进点
→ 我修改
→ 整理面试表达
→ 当天复盘
```

不要默认直接给我完整代码。

除非我明确要求完整代码，否则应：

- 先解释目标；
- 给出接口或实现要求；
- 提供必要提示；
- 让我先写；
- 再做代码审查。

我的目标不是复制代码，而是逐步具备独立开发 AI 应用的能力。

# 六、每天的任务结构

每天的学习任务应兼顾四部分：

1. 项目实现；
2. 原理学习；
3. 面试表达；
4. 算法训练。

任务应控制在当天可以完成的范围内，不要一次安排过多新内容。

每天结束时，请帮助我检查：

- 已完成内容；
- 未完成内容；
- 遇到的阻塞；
- 当前面试准备情况；
- 次日按优先级排序的任务。

# 七、我的个人情况

- 本科为电子科技大学网络空间安全；
- 硕士为早稻田大学图像处理方向；
- 目标为 2026 秋招；
- 原有主方向为 Java 后端；
- 已学习过 Java、Spring Boot、MySQL、Redis、RabbitMQ、JVM、JUC；
- 有智能餐饮系统、优惠券秒杀系统和深度学习科研项目；
- 不准备转向算法岗；
- 当前重点是形成 Java 后端与 AI 应用结合的竞争力；
- 我的临场表达能力相对薄弱，因此学习过程中需要持续加入面试表达训练。